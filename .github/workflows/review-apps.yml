name: Deploy Review App

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened, closed]
    paths-ignore:
      - "**.md"
      - "docs/**"
      - "LICENSE"
      - ".gitignore"
      - "CHANGELOG.md"
      - "SECURITY.md"
      - "Roadmap.md"

jobs:
  deploy-review:
    name: Deploy Review App
    runs-on: blacksmith-4vcpu-ubuntu-2404
    if: github.event.pull_request.draft == false
    permissions:
      pull-requests: write
    environment:
      name: supabase-preview
    env:
      APP_NAME: blue-banded-bee-pr-${{ github.event.number }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Setup Fly.io CLI
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Ensure review app exists
        run: |
          if ! flyctl apps show "${APP_NAME}" >/dev/null 2>&1; then
            echo "Creating new review app: ${APP_NAME}"
            flyctl apps create "${APP_NAME}" --org personal
          else
            echo "Review app already exists: ${APP_NAME}"
          fi
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Configure Supabase preview database
        run: |
          if [ -z "${SUPABASE_DB_URL}" ]; then
            echo "Supabase preview database secret (SUPABASE_DB_URL) is not available." >&2
            exit 1
          fi

          echo "Setting Supabase preview database URL for ${APP_NAME}"
          flyctl secrets set DATABASE_URL="${SUPABASE_DB_URL}" --app "${APP_NAME}"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}

      - name: Deploy Review App
        run: |
          flyctl deploy \
            --app "${APP_NAME}" \
            --config .fly/review_apps.toml \
            --remote-only
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Comment PR with app URL
        uses: actions/github-script@v7
        with:
          script: |
            const appName = `blue-banded-bee-pr-${{ github.event.number }}`;
            const appUrl = `https://${appName}.fly.dev`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `🐝 **Review App Deployed!**

              **Test URL**: ${appUrl}

              📋 **Test Features:**
              1. Visit homepage: ${appUrl}/
              2. Test authentication and job creation flow
              3. Check dashboard functionality
              4. Verify database operations work correctly

              🗄️ **Database**: Supabase preview branch for this PR
              ⚠️ *Preview environment - app and database cleaned up when PR closes*`
            });

  cleanup-review:
    name: Cleanup Review App
    runs-on: blacksmith-4vcpu-ubuntu-2404
    if: github.event.action == 'closed'

    steps:
      - name: Setup Fly.io CLI
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Destroy Review App
        run: |
          APP_NAME="blue-banded-bee-pr-${{ github.event.number }}"
          flyctl apps destroy $APP_NAME --yes || echo "App may not exist"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
