version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - revive
    - ineffassign
    - gocyclo
    - misspell

formatters:
  enable:
    - gofmt
    - goimports
